import { UnifiedPrescriptionInterface } from "@/components/prescription/unified-prescription-interface";

const PrescriptionPage: React.FC = () => {
  return <UnifiedPrescriptionInterface context="admission" />;
};

export default PrescriptionPage;
